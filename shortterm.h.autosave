#ifndef SHORTTERM_H
#define SHORTTERM_H

#include "account.h"

class ShortTerm : public Account
{
    public:
        shortTerm();
        shortTerm(int id,BigInteger cash);
        virtual ~shortTerm();

        // financial operations
        virtual void deposite(BigInteger);
        virtual bool withdraw(BigInteger);
        virtual int getBalance();
        
        // display operations
        virtual int getID();
        virtual int benefit();
        
};

#endif // SHORTTERM_H
